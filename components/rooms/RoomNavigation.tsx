'use client'

// V1 floorplan upload removed - this component is now deprecated
// It previously handled the old rectangle-based floorplan upload modal
// New V2 floorplan editor is accessed via /floorplan?homeId=<id>

export function RoomNavigation() {
  return null
}
