# Minimal dependencies for rembg without pymatting (which requires numba/llvmlite)
# These are Python 3.12 compatible
pillow>=10.0.0
onnxruntime>=1.18.0
numpy>=1.26.0
pooch>=1.8.0
tqdm>=4.66.0
jsonschema>=4.20.0

# Rembg without optional dependencies
rembg
